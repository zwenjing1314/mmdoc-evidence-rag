# 开题报告中“项目实践”部分修改建议

## 一、结论

你之前给导师看的“项目实践”写法比较概括：

```text
已完成一个多模态文档问答小项目，初步实现了文档上传、文本抽取、向量检索和基于检索结果生成回答的基本流程。
```

这句话太空，导师不容易看出你具体做了什么。

根据你提供的项目记录，应该把它改成更具体的 DocVQA 项目经历：

```text
基于 SP-DocVQA 数据集，完成 OCR 解析、answer alignment、span 数据构建、OCR rule baseline、LayoutLMv3 训练、推理评测、误差分析和 Gradio Demo。
```

也就是说，刚刚那版“项目实践”建议替换。

## 二、为什么要替换

导师说“内容太空”，主要是因为原文没有写清楚：

1. 用了什么数据集；
2. 做了什么任务；
3. 实现了哪些模块；
4. 用了什么模型；
5. 有什么实验结果；
6. 这个项目和现在论文有什么关系。

你的旧项目其实能提供这些信息：

| 维度 | 具体内容 |
|---|---|
| 数据集 | SP-DocVQA |
| 任务 | 文档视觉问答 DocVQA |
| 数据处理 | annotation / OCR / image 解析，answer alignment，span subset 构建 |
| baseline | OCR rule baseline |
| 模型 | LayoutLMv3 |
| 输入 | image + question + OCR words + bounding boxes |
| 监督方式 | span-based QA |
| 实验结果 | EM 0.564 / ANLS 0.651 |
| 对照结果 | OCR baseline EM 0.123 / ANLS 0.161 |
| 展示 | Gradio Demo |
| 分析 | table/list、form、layout 等题型误差分析 |

这些内容比“做过一个多模态问答项目”具体得多。

## 三、建议替换位置

建议替换你开题报告中的这一段：

```text
其次，在项目实践方面，已完成一个多模态文档问答小项目，初步实现了文档上传、文本抽取、向量检索和基于检索结果生成回答的基本流程。该项目虽然规模较小，但暴露了传统文档问答系统的几个问题：一是 PDF 文本抽取后容易丢失表格结构；二是固定长度切块难以保证证据片段完整；三是系统能够生成答案，但往往无法明确指出答案来自哪一页、哪个区域；四是生成结果缺乏证据支持性判断。这些问题直接推动了本文从“普通文档问答”转向“多粒度证据检索与可信生成”的研究选题。
```

## 四、可直接替换版本

建议改成下面这一版。

```text
其次，在项目实践方面，已完成一个面向 DocVQA 场景的多模态文档问答项目。该项目基于 SP-DocVQA 数据集，从数据处理、基线方法、模型训练到评测展示构建了较完整的实验流程。具体而言，项目首先对原始 annotation、OCR 结果和文档图像进行解析与对齐，完成 answer alignment 和 span-based QA 监督数据构建；随后实现 OCR rule baseline，用于基于 OCR 文本进行候选行检索、关键词匹配和字段值抽取；在此基础上，使用 LayoutLMv3 构建多模态文档问答模型，使模型同时利用文档图像、问题文本、OCR words 和 bounding boxes 预测答案 span。

在实验方面，该项目在 full train/full val 构建的可监督子集上完成训练与验证，其中训练集包含 31222 条样本，验证集包含 4274 条样本。最佳 LayoutLMv3 单模型在验证集上达到 EM 0.564、ANLS 0.651，明显优于 OCR rule baseline 的 EM 0.123、ANLS 0.161。项目还实现了 baseline、model 和 hybrid 方法的统一评测，并结合 table/list、form、layout 等题型进行误差分析，最后通过 Gradio Demo 展示文档图像、问题、标准答案和模型预测结果。

该项目使本文前期掌握了文档图像、OCR 文本、版面坐标和问答监督信号之间的关系，也暴露出传统文档问答方法的局限：一是 OCR 文本或固定 span 预测难以处理多页长文档证据检索；二是模型可以输出答案，但缺少页码、区域和证据节点级引用；三是答案是否真正被证据支持仍缺少验证机制。因此，本文在该项目基础上进一步将研究重点从单页 DocVQA 扩展到多模态长文档中的多粒度证据检索、证据定位和可信生成。
```

## 五、如果想再精简一点

如果开题报告篇幅不够，可以用这个精简版本。

```text
其次，在项目实践方面，已完成一个基于 SP-DocVQA 的多模态文档问答项目。该项目完成了 annotation、OCR 和文档图像解析，构建 answer alignment 与 span-based QA 监督数据，实现 OCR rule baseline，并基于 LayoutLMv3 训练文档问答模型。模型输入包括文档图像、问题文本、OCR words 和 bounding boxes，输出答案 span。在 full train/full val 构建的可监督子集上，最佳 LayoutLMv3 单模型达到 EM 0.564、ANLS 0.651，明显优于 OCR baseline 的 EM 0.123、ANLS 0.161。项目还完成了 baseline、model 和 hybrid 方法对比、误差分析和 Gradio Demo 展示。

该项目使本文前期熟悉了文档图像、OCR 文本、版面坐标和问答监督之间的关系，同时也暴露出单页文档问答方法在长文档场景中的不足：缺少多页证据检索、细粒度证据定位和答案支持性验证。因此，本文进一步将研究重点从 DocVQA 扩展到多模态长文档的多粒度证据检索与可信生成。
```

## 六、刚刚那版还要不要替换

要替换。

但不是整段“前期研究基础”都替换，而是只替换其中“项目实践方面”的那一段。

原来的“前期研究基础”结构可以保留：

```text
首先，在文献阅读方面……
其次，在项目实践方面……
最后，在前期实验方面……
```

其中：

1. 文献阅读部分保留；
2. 项目实践部分替换成 DocVQA 项目具体描述；
3. 中文年报前期实验部分保留。

也就是说，最终应该形成三层前期基础：

```text
文献基础：LayoutLM / LiLT / Donut / 文档处理综述
项目基础：SP-DocVQA + LayoutLMv3 文档问答项目
实验基础：中文年报 20 份 PDF + 99304 nodes + 160 QA + 三组检索实验
```

这个结构非常适合回应导师说的“缺少实实在在的研究内容”。

## 七、前期研究基础完整融合版

如果你想直接整段替换“（六）前期研究基础”，可以用下面这个完整版本。

```text
（六）前期研究基础

在开题前，本文已围绕文档智能、多模态文档问答和长文档证据检索开展了文献调研、项目实践和初步实验，为后续研究奠定了基础。

首先，在文献阅读方面，已重点阅读文档智能与版面理解相关研究，包括 LayoutLM、LayoutLMv2、LayoutLMv3 等 Layout 系列模型，了解文本、版面位置和视觉信息联合建模的基本思路；阅读 LiLT 等轻量级文档理解模型，了解跨语言文档理解和结构信息迁移方法；阅读 Donut 等 OCR-free 文档理解方法，了解端到端文档图像理解路线；同时阅读多篇文档智能、表格识别、多模态文档理解和视觉信息抽取综述，对文档解析、版面分析、表格结构识别、视觉文档检索和文档问答等方向形成了初步认识。这些文献调研帮助本文明确了一个基本判断：多模态长文档 RAG 不能简单依赖固定长度文本切块，而需要保留页面、版面、表格和证据来源等结构信息。

其次，在项目实践方面，已完成一个面向 DocVQA 场景的多模态文档问答项目。该项目基于 SP-DocVQA 数据集，从数据处理、基线方法、模型训练到评测展示构建了较完整的实验流程。具体而言，项目首先对原始 annotation、OCR 结果和文档图像进行解析与对齐，完成 answer alignment 和 span-based QA 监督数据构建；随后实现 OCR rule baseline，用于基于 OCR 文本进行候选行检索、关键词匹配和字段值抽取；在此基础上，使用 LayoutLMv3 构建多模态文档问答模型，使模型同时利用文档图像、问题文本、OCR words 和 bounding boxes 预测答案 span。在 full train/full val 构建的可监督子集上，最佳 LayoutLMv3 单模型在验证集上达到 EM 0.564、ANLS 0.651，明显优于 OCR rule baseline 的 EM 0.123、ANLS 0.161。项目还实现了 baseline、model 和 hybrid 方法的统一评测，并结合 table/list、form、layout 等题型进行误差分析，最后通过 Gradio Demo 展示文档图像、问题、标准答案和模型预测结果。

该项目使本文前期掌握了文档图像、OCR 文本、版面坐标和问答监督信号之间的关系，也暴露出传统文档问答方法的局限：一是单页或短文档问答方法难以直接处理多页长文档证据检索；二是模型可以输出答案，但缺少页码、区域和证据节点级引用；三是答案是否真正被证据支持仍缺少验证机制。因此，本文在该项目基础上进一步将研究重点从单页 DocVQA 扩展到多模态长文档中的多粒度证据检索、证据定位和可信生成。

最后，在前期实验方面，本文已围绕中文上市公司年度报告完成了一个小规模实验闭环。当前实验处理了 20 份公开中文年度报告 PDF，解析得到 5327 个页面，并进一步构建 99304 个段落、表格块和表格行级证据节点。在此基础上，构建并人工修订了 160 条中文年报问答样本，问题覆盖报告年度、报告标题、营业收入、归属于上市公司股东的净利润、经营活动产生的现金流量净额、研发投入、资产总额、风险文本和营业收入同比变化等类型。实验中已实现 BM25-page、Dense-page 和 Page -> Region 三类检索方法，并采用 Page Recall@k、MRR、nDCG@k 和 Region Hit@k 等指标进行评价。初步结果表明，Dense-page 的页级召回明显优于 BM25-page，而 Page -> Region 方法在保持相近页级检索效果的同时，能够进一步定位细粒度证据节点，说明“页面级粗召回—区域级细定位”的研究路线具有可行性。

通过上述文献调研、DocVQA 项目实践和中文年报前期实验，本文已具备进一步开展多粒度证据表示、页面—区域协同检索和可信生成验证研究的基础。
```

## 八、前期研究与实验基础表也要同步改

之前建议的表格里“项目实践”那一行也要替换。

建议改成：

| 类别 | 已完成内容 | 对本文的作用 |
|---|---|---|
| 文献阅读 | 阅读 LayoutLM 系列、LiLT、Donut、文档智能综述、表格识别综述等 | 明确文档结构、版面信息和多模态表示的重要性 |
| 项目实践 | 基于 SP-DocVQA 完成 OCR baseline、answer alignment、LayoutLMv3 训练、EM/ANLS 评测和 Gradio Demo | 熟悉文档图像、OCR 文本、bbox 和问答监督建模，发现单页 DocVQA 向长文档证据检索扩展的必要性 |
| 数据处理 | 完成 20 份中文年报 PDF 解析 | 验证真实中文长文档处理流程 |
| 证据节点 | 构建 5327 个页面和 99304 个证据节点 | 验证多粒度证据表示可行 |
| QA 标注 | 构建并人工修订 160 条中文年报问答 | 为检索和证据定位评价提供标准 |
| 检索实验 | 完成 BM25-page、Dense-page、Page->Region 三组实验 | 初步验证页面—区域协同检索路线 |
| 评价结果 | Page->Region 的 Region Hit@5 达到 0.2000 | 说明细粒度证据定位具有可行性和继续优化空间 |

## 九、答辩时怎么讲这个项目实践

如果导师问“你说做过文档问答项目，具体是什么”，可以这样回答：

```text
我之前做过一个基于 SP-DocVQA 的多模态文档问答项目。这个项目不是只调模型，而是从数据处理到评测展示做了完整流程。我先解析 annotation、OCR 和文档图像，做 answer alignment，把样本构造成 span-based QA 监督格式；然后实现了 OCR rule baseline；之后用 LayoutLMv3 训练模型，让模型同时利用 image、question、OCR words 和 bounding boxes 来预测答案 span。最后在 full train/full val 构建的可监督子集上，模型达到 EM 0.564、ANLS 0.651，明显优于 OCR baseline。这个项目让我意识到单页文档问答虽然能回答问题，但缺少长文档证据检索、页码区域引用和答案支持性验证，所以我现在的论文进一步研究多粒度证据检索和可信生成。
```

## 十、注意不要过度包装

不要写成：

```text
已完成多模态长文档 RAG 系统。
```

因为这个旧项目是 DocVQA，不是长文档 RAG。

更稳的写法是：

```text
已完成一个面向 DocVQA 的多模态文档问答项目，为本文后续研究长文档证据检索和可信生成提供了项目实践基础。
```

不要写成：

```text
该项目已经解决了可信生成问题。
```

应该写：

```text
该项目暴露出答案缺少证据引用和支持性验证的问题，推动本文进一步研究可信生成。
```
