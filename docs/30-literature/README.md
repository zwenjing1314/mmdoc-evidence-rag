# 文献

> Status: working | Scope: 相关工作 | SSOT: 是

要求：每篇一句话结论 + 与本文关系（是否用 OCR/BBox、布局注入位置、是否更新局部 patch、是否报告区域定位）。

- Beyond Bag-of-Patches：GPT-5 全局描述 + global token，不用 OCR/BBox；本文做 OCR/BBox 到 patch 的显式空间软融合（见 `docs/literature/12_Bag-of-patches阅读笔记.md`，待压缩）。
- RegionRAG / Snappy / ColParse / Argus：待按上式补一行。
