# Freeze 2026-09-12（第 1 步：冻结现状，不删除）

## 1. Git 基线

- Branch: `thesis/structdocir`
- HEAD: `71112d662b8b922733133d832768ce16c80f39f0`
- HEAD message: `打算重构项目`
- Remote: `origin https://github.com/zwenjing1314/mmdoc-evidence-rag.git`（fetch/push 均存在）
- 已有 tag（冻结前）: `paper-v0.1-data-split`
- 本次冻结 tag（待打）: `archive-before-cleanup-20260912`
- 状态判定：`git diff --stat` 为空，无已跟踪文件修改；`git status --short` 仅有 `??` 未跟踪文件（第二步新骨架 + 本冻结文件），无 `M/A/D`。因此 tag 可覆盖全部已跟踪代码，未跟踪文件靠 `git add + commit` 纳入后再打 tag。

证据文件（本目录）：

```text
FREEZE-branch.txt      # git branch --show-current
FREEZE-head.txt        # git rev-parse HEAD
FREEZE-log.txt         # git log --oneline -10
FREEZE-status.txt      # git status --short
FREEZE-diff.txt        # git diff --stat
FREEZE-untracked.txt   # git ls-files --others --exclude-standard
```

## 2. 已完成的 ColPali smoke（EXP-001，标记为待登记，已可定位）

- Config: `configs/experiments/mmdocir_colpali_smoke.yaml`
- 内容：`experiment_name=mmdocir_colpali_smoke, dataset=mmdocir_evaluation, retriever.type=colpali_page, search_scope=document, model=vidore/colpali, top_k=5`
- 最新 run：`runs/retrieval/mmdocir_colpali_smoke/20260912_123624`（`latest.txt` 指向该目录，Windows 下为 `latest.txt` 而非软链接）
- `run_info.json`：`retriever_type=colpali_page, hits=30, actual_retrievers=[colpali_page], data_split=null`
- `metrics.json`（`evaluate` 产物，原样抄）：`page_recall@1=0.8333, @5=1.0, @10=1.0, mrr=0.9167, ndcg@5=0.8945, ndcg@10=0.8945, region_hit@5=0.0`
- 产物清单：`config.json, predictions.parquet, run_info.json, metrics.json, errors.csv, summary.md` 齐全
- 相关资产：`configs/experiments/mmdocir_colpali.yaml`（full）、`tests/test_colpali_integration.py`、`docs/reproduction/colpali_vidore_v1_reproduction_20260830.md`、`src/mmdocrag/retrieval/colpali.py`
- 注意：smoke 为小样本（hits=30），不是全量 baseline；全量 ColPali baseline 尚未登记，不可引用为正式结果。

## 3. 已有重要运行结果（只登记位置，不复制结论）

- MMDocIR BM25：`runs/retrieval/mmdocir_bm25_page/20260816_123731`（`latest.txt` 指向），正式指标见 `docs/paper/04_mmdocir_results.md`
- MMDocIR 其他 runs（目录存在，待逐个登记到 `experiments/registry.csv`）：`mmdocir_colpali/`、`mmdocir_dense_page_bge_m3/`、`mmdocir_hybrid_page_bge_m3/`、`mmdocir_layout_node_bge_m3/`、`mmdocir_colpali_smoke/`（7 个时间戳目录）
- 中文冻结 test 主表：`docs/paper/03_test_results.md` 第 4 节 16 个 run 目录（`cn_bm25_page/test/20260814_235548` 等），`runs/` 下中文目录本次未列出，需在第二步 SSOT 落实后补查
- 冻结划分：`configs/splits/cn_annual_reports_company_v1.yaml`；中文 QA 版本见 `docs/paper/01_experiment_protocol.md`（SHA 以该 SSOT 为准）
- 外部数据（不进 git，只记路径）：MMDocIR 原始集在项目外，`MMDOCIR_EVALUATION_ROOT` 指向的 `MMDocIR_Evaluation_Dataset`；`data/`, `artifacts/hf_cache`, `artifacts/colpali` 均被 `.gitignore` 忽略，不进 zip

## 4. 本地归档副本（不把十几 GB 压进 zip）

- 包 1 Git 代码：`../mmdoc-evidence-rag-code-20260912.zip`（`git archive HEAD`，仅已跟踪文件）
- 包 2 运行元数据：`../mmdoc-evidence-rag-runs-meta-20260912/`（仅 `config.json/run_info.json/metrics.json/summary.md/errors.csv` + `runs-list.txt`，不含 `*.parquet/*.pt/*.npy`）
- 验证：zip 可打开看到 `src/configs/docs`；meta 目录有 `runs-list.txt` 可定位每个 run

## 5. 完成标准自检

- [ ] `FREEZE-*.txt` 六个文件与本文件齐全，可说出分支和 HEAD
- [ ] smoke 最新 run 目录可用路径找到，`run_info.json/metrics.json` 可读
- [ ] `../mmdoc-evidence-rag-code-20260912.zip` 存在可打开
- [ ] 未删除、未改名、未改任何 `configs/*.yaml`
- [ ] `git commit + git tag archive-before-cleanup-20260912` 完成
