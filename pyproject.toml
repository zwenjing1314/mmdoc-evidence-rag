[project]
name = "mmdoc-evidence-rag"
version = "0.1.0"
description = "Experiments for multi-granularity evidence retrieval and trustworthy generation over multimodal long documents."
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
  "accelerate",
  "datasets",
  "duckdb",
  "faiss-cpu",
  "huggingface-hub",
  "loguru",
  "numpy",
  "opencv-python",
  "openpyxl",
  "pandas",
  "pdfplumber",
  "pillow",
  "polars",
  "pyarrow",
  "pydantic>=2",
  "pydantic-settings",
  "pymupdf",
  "python-dotenv",
  "pyyaml",
  "rank-bm25",
  "ranx",
  "rich",
  "scikit-learn",
  "scipy",
  "sentence-transformers",
  "tokenizers",
  "tqdm",
  "transformers",
  "typer",
]

[dependency-groups]
dev = [
  "ipykernel",
  "jupyterlab",
  "mypy",
  "pre-commit",
  "pytest",
  "pytest-cov",
  "ruff",
]

[tool.uv]
package = true

[project.scripts]
mdr = "mmdocrag.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mmdocrag"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-q"

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
strict = false
