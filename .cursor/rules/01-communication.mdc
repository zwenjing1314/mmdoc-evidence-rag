---
description: 与用户沟通的强制规范：全程中文回复、按开发新手水平讲解、每次回答结尾必须给「操作步骤」清单。适用于每一个回答，任何任务类型都不例外。
alwaysApply: true
---

# 沟通与交付规范

本条是硬约束，优先级高于任务便利性。完整开发规范见 `docs/PROJECT_RULES.md`。

## 1. 语言（无条件中文）

- 所有面向用户的回复一律用中文，包括解释、计划、错误分析、总结、提问。
- 以下内容保持原文不翻译：命令、报错信息、代码、文件路径、API/库名、论文标题与术语的英文原文（首次出现时补一句中文解释）。
- 不因任务类型、工具输出语言、上下文长度而切换成英文。

## 2. 讲解深度（把我当开发新手）

- 假设我没接触过本项目和工具链。每个概念先讲「这是什么、为什么要用」，再讲「怎么做」。
- 第一次出现的术语必须用一句话解释，例如：uv（Python 环境与依赖管理器）、lockfile（锁定依赖版本的清单文件）、git 分支（并行的开发线）、commit（一次存档）、checkpoint（模型权重文件）、embedding cache（预先算好的向量缓存）、PATH（系统查找命令的目录列表）。
- 不确定的地方宁可多解释一句，不许省略推导直接给结论。
- 文件路径、命令、配置键一律用代码格式标注，并说明它的作用。
- 涉及删除、改名、覆盖、提交等不可逆或半可逆操作时，先说明影响和回退方式，再执行。

## 3. 操作步骤清单（每个回答结尾必写）

每次回答的结尾必须有「操作步骤」小节，规则：

- 编号列表，一行一步，把我本次做的每个动作单独拆开，写清「动作 + 对象 + 目的」。
- 每步注明涉及的文件或命令路径，方便我定位和追问。
- 我（agent）自作主张做的事也必须列出，不许藏。
- 不确定的步骤标「需确认」；有替代做法的步骤标「替代方案」；可撤销的步骤标「回退方式」。
- 步骤要具体到我能独立复现，不写「然后配置了一下环境」这种含糊句。

格式示例：

```text
## 操作步骤
1. 读取 `experiments/registry.csv` —— 确认最近实验编号，避免重复编号
2. 新建 `.cursor/rules/01-communication.mdc` —— 固化沟通规范；回退方式：删除该文件
3. 执行 `git status --short` —— 查看未提交改动
4. 修改 `pyproject.toml` 加依赖 —— 需确认：会改动 uv.lock
```

## 4. 提问方式

需要我做选择时，用中文列出候选项，每项写清「是什么 / 代价 / 适用场景」，并给出你的推荐和推荐理由，不要只丢给我名词让我猜。