---
description: 引入任何新技术、新依赖、新工具、新模型或改变既有技术栈之前，必须先向用户说明并等待明确批准。涉及安装、换模型、换框架、改工作流时必读本条。
globs:
alwaysApply: true
---

# 新技术准入规范（先批准，后动手）

## 什么算「新技术」，需要先批准

- 新增或替换 Python 依赖：改 `pyproject.toml`、`uv add`、`uv pip install`、下载新模型权重。
- 换或加技术栈组件：视觉主干模型、OCR/版式引擎、向量库、实验跟踪工具、评测库。
- 引入新工具与工作流：任务脚本方案（如改用 Make/Nox）、环境管理方式、CI、格式化/lint 规则集、目录结构的新增顶层约定。
- 新的算法/训练做法：LoRA、蒸馏损失、难负例采样策略、新的融合结构变体。
- 新的外部服务：网络下载数据集、第三方 API、远程 GPU。

## 不需要批准的（避免什么都来问）

- 读文件、搜索、跑已在 `docs/02-commands.md` 登记过的命令、跑 `check`。
- 在现有技术栈内实现文档已规定的模块（例如按 `docs/SMALL_PAPER_DEV_SPEC.md` 第 10 节新建 `src/mmdocrag/layout/`）。
- 修 bug、改注释、补测试，且不改依赖与接口。

## 批准流程（顺序不许颠倒）

1. 停下，不要在探索阶段顺手安装任何东西。
2. 用中文说明四件事：
   - 要解决什么问题，为什么现有方案不够；
   - 候选方案（含「不引入」这个选项）与各自代价；
   - 引入后的连锁影响：依赖锁定、显存、可复现性、旧结果是否作废、要改哪些文档；
   - 回退方式。
3. 明确问一句「是否同意引入 X」，等用户回复，不要把沉默当同意。
4. 批准后按 `docs/PROJECT_RULES.md` 登记流程执行：改 `pyproject.toml` → `uv sync` → 更新 `docs/01-quickstart.md` → `CHANGELOG.md` 记一条。
5. 批准只在当次任务范围内有效；换任务或换用途要重新问。

## 禁止事项

- 禁止先斩后奏（「我已经装好了，就这样吧」）。
- 禁止为了「快速验证一下」绕过 uv 用临时环境装包；临时探索也必须先问，且结论若要保留，回填 `pyproject.toml`。
- 禁止把「文档里提过某方法」当成用户已批准使用该方法的工具或依赖。
